# UI Polish and Consistency Pass

## Completed in this pass
- Unified focus styles in login/reception forms
- Replaced raw key placeholders with explicit field labels
- Added mandatory marker (*) for required fields
- Added loading feedback for register/book actions
- Added consistent inline error and success messaging

## Remaining optional polish items
- Convert sidebar anchors to router `Link` for full SPA navigation consistency
- Add reusable form input component with shared validation states
- Add toast notifications for long-running operations
